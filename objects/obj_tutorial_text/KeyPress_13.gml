/// @description DELETE - replaced by function skipCurrent()

/*
if !(drawing && text != "") return;

if (waiting) {
	drawNext();
	return;
}

position = string_length(text) + 1;
drawnText = text;
waiting = true;
*/