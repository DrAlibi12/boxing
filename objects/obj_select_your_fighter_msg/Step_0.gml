/// @description Insert description here
// You can write your code in this editor

offset = approach(offset, textWidth, 1);

if (offset >= textWidth) {
	offset = 0;
}

xTopOffset = - offset;
xBotOffset = offset;
